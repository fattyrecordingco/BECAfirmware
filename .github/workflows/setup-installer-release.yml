name: Build Setup Installer

on:
  workflow_dispatch:
  push:
    tags:
      - "setup-v*"
  release:
    types: [published]

permissions:
  contents: write

jobs:
  build-installers:
    if: |
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'release' && startsWith(github.event.release.tag_name, 'setup-v')) ||
      (github.event_name == 'push' && startsWith(github.ref_name, 'setup-v'))
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, macos-latest, ubuntu-22.04]

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: apps/beca-setup/package.json

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install Linux app dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libgtk-3-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            patchelf

      - name: Install JS dependencies
        working-directory: apps/beca-setup
        run: npm install

      - name: Build flasher sidecar
        run: cargo build -p beca-flasher --release -vv

      - name: Build bridge sidecar (Windows)
        if: runner.os == 'Windows'
        run: cargo build -p beca-bridge --release -vv

      - name: Build bridge sidecar (macOS/Linux)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          set -euo pipefail
          log_file="$(mktemp)"
          if cargo build -p beca-bridge --release -vv 2>&1 | tee "$log_file"; then
            exit 0
          fi
          echo "First beca-bridge build failed, retrying once..."
          sleep 5
          if cargo build -p beca-bridge --release -vv 2>&1 | tee -a "$log_file"; then
            exit 0
          fi
          tail_file="$(mktemp)"
          tail -n 120 "$log_file" > "$tail_file" || true
          escaped_tail="$(sed -e 's/%/%25/g' -e 's/\r/%0D/g' -e 's/\n/%0A/g' "$tail_file")"
          echo "::error title=beca-bridge build failed::${escaped_tail}"
          {
            echo "### beca-bridge build failure"
            echo
            echo '```text'
            cat "$tail_file"
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY" || true
          exit 1

      - name: Install espflash sidecar
        run: cargo install espflash --locked --version 4.2.0

      - name: Download esptool sidecar (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $version = "5.2.0"
          $zip = Join-Path $env:RUNNER_TEMP "esptool-v$version-windows-amd64.zip"
          $extract = Join-Path $env:RUNNER_TEMP "esptool-v$version-windows-amd64"
          Invoke-WebRequest -Uri "https://github.com/espressif/esptool/releases/download/v$version/esptool-v$version-windows-amd64.zip" -OutFile $zip
          if (Test-Path $extract) { Remove-Item $extract -Recurse -Force }
          Expand-Archive -Path $zip -DestinationPath $extract -Force
          $exe = Get-ChildItem $extract -Recurse -Filter "esptool.exe" | Select-Object -First 1 -ExpandProperty FullName
          if (-not $exe) { throw "esptool.exe not found in downloaded archive." }
          Copy-Item $exe "$env:RUNNER_TEMP\\esptool.exe" -Force

      - name: Copy sidecars (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          Copy-Item target\release\beca-bridge.exe apps\beca-setup\src-tauri\binaries\beca-bridge.exe -Force
          Copy-Item target\release\beca-flasher.exe apps\beca-setup\src-tauri\binaries\beca-flasher.exe -Force
          Copy-Item $env:USERPROFILE\.cargo\bin\espflash.exe apps\beca-setup\src-tauri\binaries\espflash.exe -Force
          Copy-Item $env:RUNNER_TEMP\esptool.exe apps\beca-setup\src-tauri\binaries\esptool.exe -Force

      - name: Copy sidecars (macOS/Linux)
        if: runner.os != 'Windows'
        run: |
          cp target/release/beca-bridge apps/beca-setup/src-tauri/binaries/beca-bridge
          cp target/release/beca-flasher apps/beca-setup/src-tauri/binaries/beca-flasher
          cp ~/.cargo/bin/espflash apps/beca-setup/src-tauri/binaries/espflash
          chmod +x apps/beca-setup/src-tauri/binaries/beca-bridge
          chmod +x apps/beca-setup/src-tauri/binaries/beca-flasher
          chmod +x apps/beca-setup/src-tauri/binaries/espflash

      - name: Build Tauri bundle
        working-directory: apps/beca-setup
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm run tauri build

      - name: Upload installer artifacts
        uses: actions/upload-artifact@v4
        with:
          name: beca-setup-${{ matrix.os }}
          path: target/release/bundle/**
          if-no-files-found: error

      - name: Attach artifacts to GitHub Release
        if: github.event_name == 'release'
        uses: softprops/action-gh-release@v2
        with:
          files: |
            target/release/bundle/**/*.msi
            target/release/bundle/**/*.exe
            target/release/bundle/**/*.dmg
            target/release/bundle/**/*.AppImage
            target/release/bundle/**/*.deb

  sync-installer-mirror:
    needs: build-installers
    if: always() && needs.build-installers.result != 'cancelled' && github.repository == 'fattyrecordingco/BECAfirmware'
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout master
        uses: actions/checkout@v4
        with:
          ref: master

      - name: Download installer artifacts from this run
        uses: actions/download-artifact@v4
        with:
          pattern: beca-setup-*
          merge-multiple: true
          path: ${{ runner.temp }}/beca-setup-artifacts

      - name: Sync cross-platform installer mirror
        shell: bash
        run: |
          set -euo pipefail

          artifact_root="${RUNNER_TEMP}/beca-setup-artifacts"
          mirror_root="${GITHUB_WORKSPACE}/apps/beca-setup/dist-installer"
          staged_root="${RUNNER_TEMP}/beca-setup-staged-mirror"

          rm -rf "${staged_root}"
          mkdir -p "${staged_root}/windows" "${staged_root}/macos" "${staged_root}/linux"

          windows_count=0
          macos_count=0
          linux_count=0

          while IFS= read -r -d '' file; do
            lower="$(echo "${file}" | tr '[:upper:]' '[:lower:]')"
            case "${lower}" in
              *.dmg)
                cp "${file}" "${staged_root}/macos/"
                macos_count=$((macos_count+1))
                ;;
              *.appimage|*.deb)
                cp "${file}" "${staged_root}/linux/"
                linux_count=$((linux_count+1))
                ;;
              *.exe|*.msi|*.zip|*webview2loader.dll)
                cp "${file}" "${staged_root}/windows/"
                windows_count=$((windows_count+1))
                ;;
            esac
          done < <(find "${artifact_root}" -type f -print0)

          echo "Installer files copied -> windows:${windows_count} macos:${macos_count} linux:${linux_count}"
          if [ "${windows_count}" -eq 0 ] || [ "${macos_count}" -eq 0 ] || [ "${linux_count}" -eq 0 ]; then
            echo "Missing Windows, macOS, or Linux artifacts. Mirror update aborted."
            exit 1
          fi

          rm -rf "${mirror_root}/windows" "${mirror_root}/macos" "${mirror_root}/linux"
          mkdir -p "${mirror_root}/windows" "${mirror_root}/macos" "${mirror_root}/linux"
          cp -a "${staged_root}/windows/." "${mirror_root}/windows/"
          cp -a "${staged_root}/macos/." "${mirror_root}/macos/"
          cp -a "${staged_root}/linux/." "${mirror_root}/linux/"
          touch "${mirror_root}/windows/.gitkeep" "${mirror_root}/macos/.gitkeep" "${mirror_root}/linux/.gitkeep"

      - name: Commit installer mirror updates
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          if git diff --quiet -- apps/beca-setup/dist-installer; then
            echo "No installer mirror changes."
            exit 0
          fi

          git add -A -f apps/beca-setup/dist-installer
          git commit -m "chore(setup): sync installer mirror (windows/macos/linux)"
          git push origin HEAD:master
