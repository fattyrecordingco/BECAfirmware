{
  "name": "beca-setup",
  "version": "0.1.5",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "verify:browser": "node ./scripts/verify-browser.mjs",
    "test:ui": "playwright test",
    "sync:windows": "node ./scripts/sync-windows-artifacts.mjs",
    "release:windows": "tauri build && npm run sync:windows",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@tauri-apps/cli": "^2.11.0",
    "agent-browser": "^0.26.0",
    "axe-core": "^4.11.4",
    "vite": "^8.0.10"
  }
}
