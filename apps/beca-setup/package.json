{
  "name": "beca-setup",
  "version": "0.1.4",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "sync:firmware": "node ./scripts/sync-bundled-firmware.mjs",
    "sync:windows": "node ./scripts/sync-windows-artifacts.mjs",
    "release:windows": "npm run sync:firmware && tauri build && npm run sync:windows",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.2.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.2.5",
    "vite": "^5.4.11"
  }
}
